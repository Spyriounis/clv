"""
Placeholder test suite.

A single always-true test so the CI pipeline (linting + unit tests) can be
validated end-to-end before any real model code exists.
"""


def test_placeholder() -> None:
    assert True
